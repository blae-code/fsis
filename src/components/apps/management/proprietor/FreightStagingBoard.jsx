import React from 'react';
import { FREIGHT_STAGES, stageLabel, riskColor } from '@/lib/warehouseUtils';

export default function FreightStagingBoard({ crates = [], onStage }) {
  return <div className="border p-4 font-mono" style={{ borderColor: '#3A2F20', background: '#0A0806' }}><div className="mb-3"><p className="text-[10px] tracking-[0.2em]" style={{ color: '#8A8F45' }}>FREIGHT STAGING</p><h3 className="text-sm font-bold" style={{ color: '#F2EADC' }}>Pick • pack • load pipeline</h3></div><div className="grid md:grid-cols-5 gap-2">{FREIGHT_STAGES.map((stage)=><div key={stage} className="border min-h-28 p-2" style={{ borderColor: '#2A2118', background: '#080604' }}><p className="text-[9px] tracking-[0.14em] mb-2" style={{ color: '#8A7E6C' }}>{stageLabel(stage)}</p>{crates.filter((c)=> (c.stage || 'intake') === stage).map((c)=><div key={c.id} className="border p-2 mb-2 text-[10px]" style={{ borderColor: riskColor(c.risk_level), background: '#100C08' }}><div className="flex justify-between gap-2"><b style={{ color: '#E0A22E' }}>{c.crate_code}</b><span>{c.scu_used || 0} SCU</span></div><p className="truncate" style={{ color: '#A89C8A' }}>{c.destination || c.location_code || 'UNASSIGNED'}</p><select value={c.stage || 'intake'} onChange={(e)=>onStage(c, e.target.value)} className="mt-2 w-full bg-transparent border px-1 py-1" style={{ borderColor: '#3A2F20', color: '#D8CFC0' }}>{FREIGHT_STAGES.map((s)=><option key={s} value={s}>{stageLabel(s)}</option>)}</select></div>)}</div>)}</div></div>;
}